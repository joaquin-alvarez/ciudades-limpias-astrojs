---
const bgImageUrl = `${import.meta.env.BASE_URL}/images/hero-bg.webp`;
---

<section class="hero" style={`--bg-image-url: url(${bgImageUrl})`}>
  <!-- <div class="hero-overlay"></div> -->
  <div class="container hero-content">
    <div class="logo-concurso">
      <img src={`${import.meta.env.BASE_URL}/logos/logo-concurso.svg`} alt="Logo Concurso" />
    </div>
    
    <h1 class="hero-title">
      CONCURSO<br />
      <span class="hero-subtitle">CIUDADES LIMPIAS</span>
    </h1>
    
    <p class="hero-tagline">Escuelas por el Ambiente</p>
    
    <div class="hero-prize">
      <p class="prize-text">Tu escuela puede llegar a ganar hasta <span class="star-icon">⭐</span></p>
      <p class="prize-amount">$50.000.000</p>
      <p class="prize-description">para usar en materiales, mejoras o proyectos escolares.</p>
    </div>
    
    <a href="https://escobar360.gob.ar" target="_blank" class="btn hero-btn">ESCOBAR 360</a>
  </div>
</section>

<style>
  .hero {
    position: relative;
    background-size: cover;
    background-position: center;
    height: 100vh;
    display: flex;
    align-items: center;
    color: var(--color-white);
    text-align: center;
    padding-top: 80px; /* Para compensar la altura del header */
    background-image: var(--bg-image-url);
    background-size: cover;
    background-position: center;
    }
  
  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 94, 65, 0.7); /* Verde con opacidad */
    z-index: 1;
  }
  
  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 800px;
    margin: 0 auto;
  }
  
  .logo-concurso {
    max-width: 150px;
    margin: 0 auto 2rem;
  }
  
  .hero-title {
    font-family: var(--font-title);
    font-size: 4.5rem;
    line-height: .9;
    margin-bottom: 0;
    text-transform: uppercase;
    letter-spacing: 2px;
  }
  
  .hero-subtitle {
    font-size: 3.5rem;
  }
  
  .hero-tagline {
    font-size: 1.8rem;
    margin-bottom: 2.5rem;
    font-weight: 500;
  }
  
  .hero-prize {
    margin-bottom: 3rem;
  }
  
  .prize-text {
    font-size: 1.5rem;
    font-weight: 500;
    margin-bottom: 0.5rem;
  }
  
  .star-icon {
    color: var(--color-secondary);
    display: inline-block;
    transform: scale(1.2);
  }
  
  .prize-amount {
    font-size: 3.5rem;
    font-weight: 800;
    line-height: 1;
    margin-bottom: 0.5rem;
  }
  
  .prize-description {
    font-size: 1.2rem;
  }
  
  .hero-btn {
    font-size: 1.2rem;
    padding: 15px 40px;
    text-transform: uppercase;
  }
  
  @media (max-width: 768px) {
    .hero-title {
      font-size: 3rem;
    }
    
    .hero-subtitle {
      font-size: 2.5rem;
    }
    
    .prize-amount {
      font-size: 2.8rem;
    }
  }
</style>
